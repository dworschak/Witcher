namespace = realm

character_event = {
	id = realm.1
	desc = "Name the new commander of Drakenborg"
	picture = GFX_evt_emissary
		
	trigger = {
		has_landed_title = k_redania
		has_landed_title = c_drakenborg
	}
	
	option = {
		name = "OK"
		create_character = {
			random_traits = yes
			dynasty = random
			religion = PREV
			culture = PREV
			female = no
			age = 30
		}
		new_character = {
			b_drakenborg = {
				grant_title = PREV
				succession = appointment
			}
			c_drakenborg = {
				grant_title = PREV
				succession = appointment
			}
		}
	}
}