#decisions = {
decisions = {
	invite_witcher_to_court = {
		is_high_prio = yes
		potential = {
			#any_demesne_province = { has_province_modifier = monster_infestation }
		}
		allow = {
			NOT = { any_courtier = { trait = witcher } }
			scaled_wealth = 0.2 
			NOT = { has_character_flag = invited_witcher }
		}
		effect = {
			scaled_wealth = -0.2
			set_character_flag = invited_witcher
		}
		ai_will_do = {
			factor = 0
		}
	}
	list_witchers = {
		potential = {
			witcher = { is_alive = yes }
		}
		effect = {
			character_event = { id = witcher.10 }
		}
		ai_will_do = {
			factor = 0
		}
	}
}