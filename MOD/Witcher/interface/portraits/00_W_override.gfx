# Override of vanilla sprites - must be loaded (alphabetically) before.
spriteTypes = {
	
	# Reuse p11 for pre-rendered heroes, to avoid adding a new layer to all cultures, and improve compatibility.
	spriteType = {
		name = "GFX_player_overlay"
		texturefile = "gfx\\characters\\shared\\heroes.dds"
		noOfFrames = 34
		norefcount = yes
		can_be_lowres = yes
	}
}