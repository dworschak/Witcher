# FROM is the taker
# ROOT is the target
targetted_decisions = {
	play_dice_poker = {
		ai_target_filter = self
		from_potential = {
			ai = no
			multiplayer = no
		}
		potential = {
		}
		allow = {
			# TODO check same location
			# TODO check opponent is a dice poker player
		}
		effect = {
			# Propose a game to opponent
			character_event = { id = dice_poker.0 }
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 0
		}
	}
}
