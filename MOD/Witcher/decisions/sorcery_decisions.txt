decisions = {
	convince_a_mage = {
		is_high_prio = yes
		potential = {
			has_character_flag = chose_mage_option
			has_ambition = obj_become_sorcerer
			NOT = {
				trait = sorcerer
				trait = adept
			}
		}
		allow = {
			prisoner = no
			in_command = no
			NOT = { trait = in_hiding }
			any_courtier = {
				AND = {
					trait = sorcerer
					opinion = {
						who = liege
						value = 50
					}
				}
			}
		}			
		effect = {
			ROOT = {
				random_courtier = { 
					limit = { 
						trait = sorcerer
						opinion = {
							who = liege
							value = 50
						 }
					}
					character_event = { id = sorcery.26 tooltip = "begin_ritual" } #Mage is asked for help
				}
			}
			clr_character_flag = chose_mage_option
		}
	}
	sacrifice_child = {
		is_high_prio = yes
		potential = {
			has_character_flag = chose_child_option
			has_ambition = obj_become_sorcerer
			NOT = {
				trait = sorcerer
				trait = adept
			}
		}
		allow = {
			prisoner = no
			in_command = no
			NOT = { trait = in_hiding }
			any_courtier = {
				trait = magic_talent
			}
		}			
		effect = {
			ROOT = {
				random_courtier = { 
					limit = { 
						trait = magic_talent
					}
					character_event = { id = sorcery.43 tooltip = "begin_sacrifice" } #Mage is asked for help
				}
				clr_character_flag = chose_child_option
			}
		}
	}
}
	