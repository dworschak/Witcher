
# FROM is the decision taker
# ROOT is the targeted settlement
settlement_decisions = {
	
	travel_to_holding = {
		ai = no
		filter = all
		ai_target_filter = self
		
		from_potential = {
			is_landed = no
		}
		
		potential = {
			ROOT = {
				location = {
					FROM = {
						NOT = { province = PREVPREV }
					}
				}
			}
		}
		
		allow = {
			holder_scope = {
				FROM = {
					location = {
						NOT = { distance = { who = PREVPREV value = 500 } }
					}
				}
			}
		}
		
		effect = {
			ROOT = {
				location = {
					save_event_target_as = target_province
				}
			}
			FROM = {
				clear_delayed_event = { id = landless.1 }
				add_trait = travelling
				character_event = { id = landless.1 days = 10 }
			}
		}
	}
}