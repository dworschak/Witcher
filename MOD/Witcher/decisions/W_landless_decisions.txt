
# FROM is the decision taker
# ROOT is the targeted settlement
settlement_decisions = {
	
	move_to_holding = {
		ai = no
		filter = all
		ai_target_filter = self
		
		from_potential = {
			is_landed = no
		}
		
		potential = {
			
		}
		
		allow = {
#			holder_scope = {
#				FROM = {
#					location = {
#						NOT = { distance = { who = PREVPREV value = 1000 } }
#					}
#				}
#			}
		}
		
		effect = {
			holder_scope = {
				FROM = {
					set_defacto_liege = PREV
					
					# move_character = PREV
					force_host = PREV # Move the location in UI
				}
			}
		}
	}
}